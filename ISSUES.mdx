# Volcano Client Issues
> Format (where { and } represent insertions):
> [{SELECT ' ' = not done, 'x' = done, '~' = in-progress, '-' = cancelled}] (Platform: {LIST Windows x64, Windows ARM, macOS x64, macOS ARM, Linux x64, Linux ARM}) \*\*{ISSUE}\*\* -- \*{LIST FILES}\*

[x] (Platform: Windows x64, Windows ARM) **Mouse input requires minimize/restore to lock correctly.** -- *`InputHandler.hpp`, `VulkanInit.cpp`*
[x] (Platform: Windows x64, Windows ARM) **Mouse input does not unlock.** -- *`InputHandler.hpp`, `VulkanInit.cpp`, `GUIController.cpp`*
[x] (Platform: Windows x64) **Closing the game freezes Intel graphics drivers.** -- *`RenderThread.hpp`, `RenderThread.cpp`, `VolcanoClient.cpp`*

## User-facing roadmap (post stairs/slabs)

[ ] (Platform: All) **Block breaking/placing.** No Player Digging / Block Place packet handling exists yet. -- *`NetworkClient.cpp`, `PacketIds.hpp`*
[ ] (Platform: All) **Inventory UI.** No container/window system exists; items can't be seen or managed. -- *`renderer/gui/`*
[ ] (Platform: All) **Hotbar + held-item rendering.**
[ ] (Platform: All) **Attacking/interacting with entities.**
[ ] (Platform: All) **Health/hunger HUD.** Health is now tracked internally (Set Health, for the death screen) but never displayed. -- *`GUIController.cpp`*
[ ] (Platform: All) **Tab list (player list overlay).**
[ ] (Platform: All) **Title/subtitle and a properly positioned action bar.** Action-bar-flagged messages currently just land in the chat window instead of a real HUD overlay. -- *`NetworkClient.cpp`, `GUIController.cpp`*
[ ] (Platform: All) **Block entities (signs w/ real text, chests, etc.) in the misc render pass.** -- *`renderer/misc/NonCubicMesher.cpp`*
[ ] (Platform: All) **Real entity models from `resources/minecraft-code`, replacing the placeholder box** (including other players' skins). -- *`renderer/entity/`*
[ ] (Platform: All) **Water/lava rendering.** The non-cubic render pass has a reserved slot for this, never filled in. -- *`renderer/misc/NonCubicMesher.cpp`*
[ ] (Platform: All) **Real per-block light data from the server**, replacing the current hardcoded fake per-face brightness. -- *`renderer/terrain/ChunkMesher.cpp`, `ChunkParser.cpp`*
[ ] (Platform: All) **Sprint/sneak.** -- *`TickLoop.cpp`*
[ ] (Platform: All) **Non-solid special-case blocks (water, ladders, cobwebs, ...).** Movement currently only knows binary solid/non-solid. -- *`TickLoop.cpp`, `Block.hpp`*
[ ] (Platform: All) **Sound.**
[ ] (Platform: All) **Server-pushed resource packs.** Currently ignored entirely. -- *`NetworkClient.cpp`*
[ ] (Platform: All) **Hand-swing/attack/mining animations.**